{
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.patterns": {
    "Cargo.toml": "Cargo.lock",
    "devenv.nix": ".devenv.flake.nix, devenv.lock, devenv.yaml, .pre-commit-config.yaml, .envrc"
  }
}